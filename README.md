# Проект Online_store_django

## Описание:
Проект Online_store_django — это веб-приложение интернет-магазина (каталога товаров), созданное на основе фреймворка Django. Реализован вывод товаров с динамическим усечением текста, детальные карточки продуктов с поддержкой изображений, а также форма добавления новых товаров с возможностью динамического выбора категорий из базы данных.

## Установка и запуск проекта:

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/KlimanovGrigoriy888/Online_store_django.git
   cd Online_store_django
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   * Для Windows:
     ```bash
     venv\Scripts\activate
     ```
   * Для macOS/Linux:
     ```bash
     source venv/bin/activate
     ```

4. **Установите необходимые зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Настройте базу данных PostgreSQL:**
   Выгрузите и установите PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/). С помощью утилиты pgAdmin или консоли создайте базу данных и пользователя:
   ```sql
   CREATE DATABASE <название_вашей_базы_данных>;
   CREATE USER <имя_пользователя> WITH PASSWORD '<ваш_пароль>';
   ALTER DATABASE <название_вашей_базы_данных> OWNER TO <имя_пользователя>;
   ```

6. **Настройте переменные окружения:**
   Внесите ваши данные подключения к БД (`DATABASE_NAME`, `DATABASE_USER`, `DATABASE_PASSWORD`, `DATABASE_HOST=localhost`, `DATABASE_PORT=5432`) в файл `.env.sample` и переименуйте его в `.env`.

7. **Примените миграции базы данных:**
   ```bash
   python manage.py migrate
   ```

8. **Запустите сервер разработки:**
   ```bash
   python manage.py runserver
   ```
   Сайт будет доступен в браузере по адресу: `http://127.0.0`

## Полезные команды при разработке:
* Проверить список установленных пакетов: `pip list`
* Сохранить новые установленные пакеты в зависимости: `pip freeze > requirements.txt`

## Архитектура и функционал проекта:

1. **Запуск и страницы:**
   * Проект представляет собой веб-сервер интернет-магазина, который обрабатывает и рендерит страницы каталога (`home.html`), контактов (`contacts.html`), показа деталей товара (`product_detail.html`) и формы добавления продуктов (`product_input.html`).
   
2. * Реализован принцип наследования шаблонов: создан базовый шаблон `base.html`, содержащий общую структуру сайта (метатеги, подключение стилей Bootstrap и скриптов JavaScript).
   * Изолирован повторяющийся компонент шапки сайта в отдельный файл `header.html`, который динамически подключается во все дочерние шаблоны через тег `{% include %}`.
   * Страницы каталога, контактов и форм расширяют базовый шаблон с помощью тега `{% extends %}`, что обеспечивает строгую структурированность кода и исключает дублирование верстки.
   
**Контроллеры (`catalog/views.py`):**
   * Реализована обработка GET-запросов для вывода страниц.
   * В контроллер главной страницы добавлен код, выводящий в консоль выборку из 5 последних созданных продуктов при каждом её вызове.
   * Настроена обработка POST-запросов: получение данных формы обратной связи на странице контактов, быстрый поиск товара по ID на главной странице, а также создание новых продуктов с поддержкой загрузки файлов через `request.FILES`.

4. **Маршрутизация (`catalog/urls.py`):**
   * Создана независимая маршрутизация путей и контроллеров. 
   * Для предотвращения конфликтов имён в шаблонах настроено пространство имён `app_name = 'catalog'`.

5. **Конфигурация проекта (`config/settings.py`):**
   * Зарегистрировано приложение `catalog`.
   * Настроен сбор статических файлов в директорию `static/` и обработка медиафайлов в папку `media/`.
   * Конфиденциальные данные подключения к базе данных PostgreSQL динамически загружаются из файла `.env` с помощью библиотек `os` и `python-dotenv`.
   * В главном файле `config/urls.py` добавлены настройки для сервера, позволяющие в режиме разработки (`DEBUG = True`) отдавать медиафайлы по URL.

6. **Модели и миграции (`catalog/models.py`):**
   * Созданы модели на основе базовых классов Django: `Product` и `Category`.
   * Сформированы и применены миграции базы данных с помощью команд:
     ```bash
     python manage.py makemigrations
     python manage.py migrate
     ```

7. **Административная панель (`catalog/admin.py`):**
   * Выполнена кастомная настройка отображения и фильтрации объектов классов `Product`, `Category` и `Contact` в интерфейсе админки по адресу `/admin/`.

8. **Работа с фикстурами:**
   * Создан файл фикстуры `products_fixture.json` в корне проекта.
   * Для выгрузки данных из БД в файл (с поддержкой кодировки UTF-8) используется команда:
     ```bash
     python -Xutf8 manage.py dumpdata catalog.Product catalog.Category --output products_fixture.json --indent 4
     ```
   * Для обратной загрузки данных из фикстуры в чистую базу данных используется команда:
     ```bash
     python manage.py loaddata products_fixture.json
     ```

9. **Кастомные команды управления (Management Commands):**
   В пакете `management/commands/` созданы два python-модуля для автоматизации:
   * `add_product` — команда, которая программно заполняет базу данных новыми тестовыми объектами классов `Product` и `Category`.
   * `load_fixture` — команда, которая автоматически наполняет базу данных из файла фикстуры `products_fixture.json` с помощью встроенной функции Django `call_command`.
   
   Вызов кастомных команд осуществляется через терминал:
   ```bash
   python manage.py add_product
   python manage.py load_fixture
   ```

## Тестирование:
1. На данной стадии проекта тесты не реализованы 

Команда для получения данных в консоль о покрытии тестами кода. 
```
poetry run pytest --cov

```
5. Для формирования отчета в HTML формате и создания файла htmlcov, необходимо написать команду в терминале:
```
pytest --cov=src --cov-report=html

```

## Документация:

Для получения дополнительной информации обратитесь к [заданию](https://my.sky.pro/student-cabinet/stream-lesson/199148/homework-requirements).

## Лицензия:

Этот проект не лицензирован.